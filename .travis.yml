language: php

addons:
  code_climate:
    repo_token: ""

php:
  - '5.6'
  - '7.0'
  - '7.1'
  - 'nightly'

matrix:
  allow_failures:
    - php: '5.4'
    - php: '5.5'
    - php: 'nightly'

install:
  - composer install

script:
  - composer travis

after_script:
  - ./vendor/bin/test-reporter
